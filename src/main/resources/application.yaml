spring:
  application:
    name: ElserManuelLazaroRosas

  datasource:
    url: jdbc:sqlserver://localhost:1434;databaseName=ElserDB;encrypt=false;trustServerCertificate=true
    username: sa
    password: MiPass123#
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true